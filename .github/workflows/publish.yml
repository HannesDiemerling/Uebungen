name: Deploy to Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Download and Extract Shinylive Assets Manually
        run: |
          mkdir -p /home/runner/.cache/shinylive
          wget --tries=5 --waitretry=3 -O shinylive.tar.gz https://github.com/posit-dev/shinylive/releases/download/v0.10.7/shinylive-0.10.7.tar.gz
          tar -xzf shinylive.tar.gz -C /home/runner/.cache/shinylive/

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Install Quarto Extension
        run: quarto add quarto-ext/shinylive --no-prompt

      - name: Render Quarto Project
        uses: quarto-dev/quarto-actions/render@v2

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
